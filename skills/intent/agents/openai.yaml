interface:
  display_name: "Intent"
  short_description: "Validate what a development request means before design"
  default_prompt: "Use $intent to build a shared understanding of this request."

policy:
  allow_implicit_invocation: false
