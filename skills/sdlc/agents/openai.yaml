interface:
  display_name: "SDLC"
  short_description: "Pair on delivery work through intent, design, and planning"
  default_prompt: "Use $sdlc to pair on this work end to end."

policy:
  allow_implicit_invocation: true
